# Argus 设计文档

> 定位：不是 RSS 过滤器，而是一条**以事件为中心的信息处理管线**。
> 它定时监控信息源、把碎片信息聚合成"事件"、定期写简报，
> 并记得它知道什么、主人关心什么。基于 pi（pi-agent-core + pi-ai）构建。
>
> **当前不是自主 agent**，见 §0.5。

## 0. 设计原则

1. **代码做确定性的事，agent 做判断性的事。**
   调度、抓取、入库、去重——纯代码，一个 token 都不花。
   相关性、聚类、重要度、写作——agent 的活。
2. **以"事件（Story）"为中心，不以"条目（Item）"为中心。**
   逐条推送的信息是碎的。Argus 把跨源的多条材料聚合成有生命周期的
   故事线：emerging（新出现）→ developing（发展中）→ resolved（已平息）。
3. **记忆是一等公民。**
   关注画像（memory/interests.md）、事件档案、用户反馈都持久化，
   agent 每次运行都站在"已知"之上工作。
4. **可观测、可约束。**
   模型的一切写操作都经由注册的工具完成，代码层校验参数、留运行记录；
   Web 监控台让每一轮分析可观测。

## 0.5 关于"agent"：当前的自主性边界

Argus 里的 Analyst 与 Editor 常被叫作 agent，但按通常标准它们**不是自主 agent**。
如实说明，避免文档跑在实现前面：

| 自主 agent 的特征 | Argus 现状 |
|---|---|
| 有目标，自己决定做什么 | 目标写死在系统提示里 |
| 能选择用哪个工具、用几次 | 每个角色只有一个工具，"选择"不存在 |
| 能控制自己的输入 | 完全不能，批次/字段/截断长度全由代码决定 |
| 迭代直到满意才停 | 处理完这批就结束，没有停止条件的判断 |
| 写自己的记忆 | `interests.md` 对它只读，它写不了东西给下一轮的自己 |
| 自我评估 / 纠错 | 无，`feedback` 只有人能写 |
| 调度自己 | croner 定时，它没有"何时该干活"的概念 |

**最硬的判据：两个工具全是写操作，一个读工具都没有。** 模型看到的一切
（关注画像、活跃事件、本批条目、近期反馈）都由代码预先拼进一条 user message，
它不能要求补充材料、不能查历史、不能抓正文。

这一半是刻意的。对"采集 → 聚类 → 写简报"这个闭环，确定性管线比自主 agent
更便宜、更可复现、更好调试；让模型自己决定"现在要不要去抓 RSS"没有任何收益。

自主性真正值钱的地方在别处，都还没做：
- **深挖调查**（M3）：调查长度不定、工具选择有意义、停止条件是判断题
- **源发现与评估**（M4）：发现画像里某方向覆盖不足，去找、试抓、评估、提议
- **自调优**（M4）：读自己的运行记录与用户反馈，提议修改关注画像

前提是同一件事：**得先给它读工具，和一个它能写的记忆区。**
好消息是 `StoragePort` 的读接口（`getStoryWithItems`、`getItems`、
`listRecentFeedback`）都已就位，只是还没有被暴露成工具。

## 1. 核心概念

| 概念 | 说明 |
|---|---|
| **Source** | 信息源（目前为 RSS/Atom），有健康度记录 |
| **Item** | 采集到的最小材料单元，源内按 guid 去重 |
| **Story** | 跨源聚合的事件：标题、摘要、状态、重要度 1-10 |
| **Interest Profile** | 关注画像（markdown），Analyst 判断相关性的唯一依据 |
| **Brief** | 定期合成的中文简报（Markdown），入库并落盘 |
| **Feedback** | 用户对事件的 👍👎，进入 Analyst 下轮上下文 |

## 2. 架构

```
┌─ 采集层（纯代码，调度器驱动）────────────────┐
│  RSS/Atom 轮询 → 规范化 → 剥 HTML → 去重     │→ SQLite（node:sqlite）
│  首次抓取建立静默基线，存量历史不进入分析      │
└──────────────────────────────────────────┘
                    ↓ 新条目批次
┌─ 分析层（pi agents，冷启动、无会话状态）──────┐
│  Analyst：批量消化新条目 → 对照关注画像 →     │
│           归入/新建事件（upsert_story 工具）  │
│  Editor ：取上期简报后有更新的事件 →          │
│           合成中文简报（save_brief 工具）     │
└──────────────────────────────────────────┘
                    ↓
┌─ 监控层 ─────────────────────────────────┐
│  Hono API（只读 + 反馈写入）               │
│  React 仪表盘：简报 / 事件线 / 源健康 /     │
│  agent 运行记录（含 token 用量）            │
└──────────────────────────────────────────┘
```

### 关键机制

- **状态在数据库，不在会话里。** 每轮 agent 冷启动，上下文由代码从
  SQLite 组装（关注画像 + 活跃事件 + 新条目 + 近期反馈），
  token 可控、行为可复现。
- **工具是唯一副作用出口。** Analyst 只有 `upsert_story`，Editor 只有
  `save_brief`；参数在工具实现里校验（如条目 id 必须属于本批次），
  agent 无法越权写库。
- **静默基线。** 信息源首次抓取时条目直接标记已分析，
  避免把存量历史当"新信息"灌给 Analyst（继承自 FeedSentry 的经验）。
- **积压上限。** 非首次抓取时，单源待分析条目超过
  `analysis.maxPendingPerSource` 的部分从旧到新静默丢弃。
  静默基线只挡首次抓取，挡不住"源换了 url / 久禁重启 / 批量回填历史"
  导致的 backlog 突增——那会把真正的新消息挤出批次。
- **镜像回退。** 多个源共用一个自建实例是单点故障：url 写成
  `{组名}/路径`，配置层展开成主地址 + 备用地址，抓取时依次重试。
- **失败保留重试。** Analyst 整轮出错且未产生任何归档时，
  批次保持未分析状态，下轮重试；正常轮次无论归档与否都标记消化，
  防止死循环。

## 3. 解耦

六个 workspace 包，依赖单向：

```
        ┌────────► core ◄────────┐
        │       （类型+端口）      │
   collector      storage       agents ──► pi-agent-core / pi-ai
        ▲            ▲            ▲
        └──────── server ─────────┘
              （唯一的组装根）
                    │ HTTP
                   web
```

- `core` 零依赖，只有领域类型与端口接口（`StoragePort`、`ChannelPort`）
- `collector` / `agents` 面向 `StoragePort` 编程，不认识 SQLite
- `server` 是唯一知道所有实现的组装根（配置加载、依赖注入、调度、API）
- `web` 只与 HTTP API 交互；推送通道悬置，`ChannelPort` 留好接口

## 4. 模型接入

- pi-ai `createProvider()` + `openai-completions` API 接任意 OpenAI 兼容端点
- compat 关闭 developer role / reasoning_effort，兼容各类中转与本地服务
- 当前单模型跑通；`ARGUS_ANALYST_MODEL` / `ARGUS_EDITOR_MODEL`
  环境变量已预留按角色分层（快模型跑量、强模型跑质量）

## 5. 路线

- **M1（已完成）**：采集 → Analyst 聚类 → Editor 简报闭环 + Web 监控台
  - 监控台：搜索/筛选/分页、反馈幂等与备注、调度可见、源健康排序、简报正文消毒
  - 采集加固：镜像回退、积压上限、实体解码、调度间隔修正
  - ⚠️ 闭环跑通但**只验证过极小样本**（4 条 → 1 个事件 → 1 期简报）。
    聚类质量、多轮增量归并、反馈回路都尚未被真实数据检验。
- **M2**：推送通道落地（实现 `ChannelPort`）、重大事件即时警报（评分 ≥ alertThreshold）
- **M3**：Researcher agent —— 对高分事件深挖（搜索、抓正文、交叉验证）。
  这是**第一个真正需要自主性的角色**，前提是先给模型读工具（见 §0.5）
- **M4**：自主性 —— 源发现与评估、按事件热度动态调抓取频率、每周回顾、
  让模型能写自己的记忆

## 6. 风险与对策

| 风险 | 对策 |
|---|---|
| token 成本失控 | 批处理上限（MAX_BATCHES）、摘要截断、状态放库不放会话、分层预留 |
| 简报幻觉 | 简报要求每条结论对应材料内链接；Editor 提示词禁止编造 |
| 聚类质量差 | 事件允许后续人工评估迭代提示词；运行记录展示每轮总结便于审查 |
| 源被封/失效 | 健康度记录连续失败次数，仪表盘可见 |
